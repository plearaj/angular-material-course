import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.austinsfirstionic.app',
  appName: 'ionic-angular-course',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
